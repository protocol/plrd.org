"use client";
import FeedWorkbench from "@/components/lab/FeedWorkbench";

/** Enter the workshop directly; the feed owns the one compact invitation. */
export default function Landing() {
  return <FeedWorkbench />;
}
